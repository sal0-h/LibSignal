# Level 2 single-axis ablation: hub OD + noise only.
# Demand/early-stop from the L1/L2 od_hub base; axis params from
# configs/tsc/axis_noise_world.yml (same values as realism_full).
includes:
  - configs/tsc/colight.yml
  - configs/tsc/axis_noise_world.yml
  - configs/tsc/od_hub_1800_base.yml
