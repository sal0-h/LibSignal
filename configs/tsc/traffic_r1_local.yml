includes:
  - configs/tsc/traffic_r1.yml

# Local HuggingFace Traffic-R1-3B (Public 0.1). Requires GPU for practical speed;
# CPU works but is very slow for a full 1800s episode.

model:
  backend: local
  model_path: "Season998/Traffic-R1"
  model_subfolder: "huggingface"
