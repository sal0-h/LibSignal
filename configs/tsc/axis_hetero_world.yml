# L2 single-axis: mixed fleet only.
# Same flag as realism_full_world.yml. Hub OD demand files stay type="pkw"
# (same as compound L2): heteroAdditional is loaded, then demand_train_file
# swaps the route file.
world:
  hetero: true
